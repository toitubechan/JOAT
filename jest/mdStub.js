// Stub for `.md` imports under Jest. The real content is bundled by Metro's
// md-transformer at runtime; the parser tests feed crafted markdown strings, so
// here every `.md` import just resolves to an empty string.
module.exports = "";
